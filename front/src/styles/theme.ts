/**
 * Theme utilities - thin wrapper re-exporting from lib/theme.tsx
 * All colors derive from CSS custom properties
 */

export { getColors, getChartTheme, getCandleColors, useTheme, ThemeProvider, warmColorCache } from '@/lib/theme';
export type { ThemeColors } from '@/lib/theme';
