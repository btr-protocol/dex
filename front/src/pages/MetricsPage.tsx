import { ViewEmptyState } from '@/components/ViewEmptyState';

export default function MetricsPage() {
    return (
        <ViewEmptyState
            title="Metrics"
            description="Protocol metrics will be available after mainnet indexing starts."
        />
    );
}
