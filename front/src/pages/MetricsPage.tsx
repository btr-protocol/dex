import { ViewEmptyState } from '@/components/ViewEmptyState';

export default function MetricsPage() {
    return (
        <ViewEmptyState
            title="Metrics Coming Soon"
            description="Protocol metrics and analytics will be available after a few weeks of mainnet operation."
        />
    );
}
