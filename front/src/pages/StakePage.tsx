import { ViewEmptyState } from '@components/shared/ui-utilities';

export function StakePage() {
    return (
        <ViewEmptyState
            title="Staking Coming Soon"
            description="Liquid staking and BTR rewards will be available after a few weeks of mainnet operation."
        />
    );
}
