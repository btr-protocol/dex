/**
 * Re-export type-safe utilities from SDK
 * These are maintained in @btr/dex-sdk/utils for cross-package usage
 */

export { safeJson, safeCast, isObject, hasProperty, safeGet } from '@sdk/utils';
