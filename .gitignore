# Environment variables / secrets — never commit
.env
.env.*
*.env
!.env.example
!**/.env.example
secrets/
private/
*.pem
*.key
**/mnemonic*
**/*private*key*

# Editor directories and files
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-workspace
*.sublime-project

# Dependencies
node_modules/

# Bun
.bun/
bun.lock
bun.lockb
bun-debug.log

# TypeScript
*.tsbuildinfo
dist/
build/
*.js.map

# Playwright artifacts
.playwright-mcp/

# Testing
coverage/
.nyc_output/
*.lcov

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
pnpm-debug.log*

# Foundry & Forge
# `broadcast/` is NOT ignored: evm/broadcast holds Sepolia deploy provenance and is
# tracked. evm/.gitignore scopes out the throwaway parts (31337, dry-run, Chapel).
cache/
out/
artifacts/
forge-cache/
foundry-cache/
.forge-snapshots/

# Solidity
*.abi
*.bin
*.deployed
*.solc
*.metadata.json

# Coverage reports (Solidity)
coverage.json
lcov.info

# Gas reports
.gas-snapshot
gas-report.txt
gas-snapshot.txt

# Slither
slither.db.json
.slither/

# Dumps and output files
dumps/
dump
*.dump
*.dmp
*.out

# Temporary files
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup

# OS generated files
Thumbs.db
Desktop.ini

# Python (in case of scripts)
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
env/

# Lock files (keep package manager specific ones if needed)
# Uncomment the ones you want to ignore:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Build artifacts
*.exe
*.dll
*.so
*.dylib

# Local development
.local/
local/
dev/

# Anvil state persistence
.anvil/

# Local data directories
**/.data/

# Native addons
*.node

.claude/

# ── AIMM sim (Rust) ──
sim/target/
sim/Cargo.lock
# sim/tape is its own crate (owns the nxr-sdk path dep); its Cargo.lock IS
# tracked because it is a leaf, but its build dir is not.
sim/tape/target/

# ── Research + sim data (local, regenerable market tapes — never committed) ──
research/data/
research/cache/
research/amm_data.js
research/**/data_raw/
research/**/out/
research/**/output_v2/
research/**/__pycache__/
research/**/.venv/
research/**/node_modules/

# Audit working artifacts (per-cycle scratch, never committed)
scratch/

# forge broadcast artifacts
broadcast/
