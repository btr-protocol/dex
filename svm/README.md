# BTR DEX — SVM (Solana)

Reserved for future Solana DEX runtime. The DEX protocol's EVM implementation lives in `../evm/`. This directory will hold the parallel Anchor/Pinocchio implementation of pools, router, and admin modules once the SVM port is scoped. Until then, no source code lives here.
